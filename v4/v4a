clear && echo "Bạn có chắc chắn muốn cài đặt phiên bản tự chạy không?" && echo "Nếu bạn cài đặt thì cả app termux nó sẽ chỉ cho bạn chạy 4g 0đ và không thể sử dụng những thứ khác, nên hãy cân nhắc." && echo "Mặc định neko, tun sẽ tự bật, và bạn không thể dùng lệnh khởi động nhanh ứng dụng." && echo "Và đặc biệt, bạn sẽ không thể tự động cập nhật phiên bản tiếp theo trong phiên bản chạy tự động." && echo -n "(Gõ y để cài đặt, gõ n để hủy): " && read lc && case "$lc" in "y") sleep 3 && clear && echo "Đang cài đặt" && apt update && apt upgrade -y && clear && cd && cd && cd $HOME && cd /data/data/com.termux/files/usr/bin/ && pkg install wget -y && wget https://github.com/caotu2k5/v4a/raw/main/v4a.zip && unzip v4a.zip && chmod a+x tun && chmod a+x psiphon-tunnel-core && chmod a+x mod && mv mod login && echo "Đã cài đặt thành công" && login;; "n") echo "Đã hủy." && login ;; *) echo "Vui lòng nhập lại" && login;; esac && echo ""